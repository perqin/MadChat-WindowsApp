<Page
    x:Class="MadChat.Views.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MadChat.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        <TextBlock HorizontalAlignment="Center" TextWrapping="Wrap" Text="Mad Chat" VerticalAlignment="Bottom" Style="{StaticResource HeaderTextBlockStyle}"/>
        <Grid Grid.Row="1" VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBox x:Name="UserNicknameTextBox" HorizontalAlignment="Center" Grid.Row="0" TextWrapping="Wrap" Width="288" Margin="0,12" Header="Nickname"/>
            <Button Content="Start Chatting!" HorizontalAlignment="Center" Grid.Row="1" Width="288" Margin="0,12" Click="StartChatting_Click"/>
        </Grid>
    </Grid>
</Page>
