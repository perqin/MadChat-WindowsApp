<Page
    x:Class="MadChat.Views.ChatPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MadChat.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="72"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.ColumnSpan="2" TextWrapping="Wrap" Text="Bot" VerticalAlignment="Center" Style="{StaticResource SubheaderTextBlockStyle}" Margin="16,0,0,0"/>
        <ListView Grid.Row="1" Grid.ColumnSpan="2"/>
        <AppBarButton Icon="Send" Grid.Row="2" Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Height="48" Width="48" Margin="8"/>
        <TextBox Grid.Row="2" TextWrapping="Wrap" VerticalAlignment="Center" PlaceholderText="Message here" FontSize="18.667" AcceptsReturn="True" MinHeight="48" Margin="8,8,0,8" Padding="10,4,6,4" MaxHeight="128" MinWidth="0" IsSpellCheckEnabled="False"/>
    </Grid>
</Page>
